//if(noone != self.sprite_index) self.image_alpha = fadeTween.alpha;

if(noone != self.sprite_index) {

	//if(noone != self.testTween && !instance_exists(self.testTween)) {
		//self.fadeInCallback();
	//}

/*
	if(self.fadeTween-- > 0) {
		self.image_alpha += 1 / self.fadeInDuration;
		if(self.fadeTween == 0) {
			self.fadeInComplete = true;	
		}
	}
	else if(self.fadeTween++ < 0) {
		self.image_alpha += 1 / self.fadeOutDuration;
		if(self.fadeTween == 0) {
			instance_destroy(self.id);
		}
	}
*/
	
}